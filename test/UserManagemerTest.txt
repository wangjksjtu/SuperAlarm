    package basic;
    import java.util.ArrayList;
    import static java.lang.Math.random;


    public class UserManagerText{
        private UserManager UManager;
	private User user;
        private String pw; 
        private String mail
        
        //数据库列表无法检验

        public void setPw(String pw){
            this.Pw=pw;
        }
        public boolean checkPw(){
            if (pw.length() >=6 && pw.length() <= 20) return true;
            return false;
        }
	
	UserManager(){
		UManager=UserManager();
		user=User();
	}
        
        public void setmail(String mail){
            this.Mail=mail;
        }
        public boolean validMail() {
            return this.Umanager.validMail();
        }
   
        //验证码无法检验

	public void setuser(String mail,String password,String sex,String name,int age,int vertifictionCode){
		this.user.mail=mail;
		this.user.password=password;
		this.user.sex=sex;
		this.user.name=name;
                this.user.age=age;
                this.user.vertifictionCode=vertifictionCode;
	}
	public int add(){
		this.UManager.addUser(this.user);
		return this.UManager.length;
	}
	public int delete(){
		this.UManager.deleteUser(this.user);
		return this.UManager.length;
	}
	public boolean isExist(){
		return user.UManager.isExist(this.user);
	}
	public boolean search(){
		if(this.UManager.searchUser(this.user)!=null)return true;
		return false;
	}
        //更改密码不好判断
        //setUserName & setUserAge没有必要验证
        //展示无法验证
 
}